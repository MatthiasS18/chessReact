// moves.js
export const moveRules = {
    pawn: (position, color) => {
      // retourne les cases possibles
      // exemple: en avant + prise en diagonale
    },
    knight: (position) => {
      // 8 déplacements possibles en L
    },
    bishop: (position) => {
      // diagonales infinies
    },
    rook: (position) => {
      // horizontales + verticales
    },
    queen: (position) => {
      // rook + bishop
    },
    king: (position) => {
      // 1 case dans toutes directions
    }
  };
  